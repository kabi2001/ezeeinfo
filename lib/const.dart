import 'package:flutter/material.dart';

const kEzeeinfotitle = Text(
  'Ezeeinfo cloud solutions',
  style: TextStyle(fontWeight: FontWeight.bold),
);

const kTechTeamTitle = Text(
  'Technical Team',
  style: TextStyle(
      fontWeight: FontWeight.bold, fontSize: 20.0, color: Colors.white),
);

const kSupportTeamTitle = Text(
  'Support Team',
  style: TextStyle(
      fontWeight: FontWeight.bold, fontSize: 20.0, color: Colors.white),
);

const kGpsTeamTitle = Text(
  'Gps Team',
  style: TextStyle(
      fontWeight: FontWeight.bold, fontSize: 20.0, color: Colors.white),
);

const kUpdateDetails = Text(
  'Update Details',
  style: TextStyle(fontWeight: FontWeight.bold),
);

const kBoxDecoration = BoxDecoration(
  color: Colors.blueAccent,
  borderRadius: BorderRadius.all(Radius.circular(15)),
);

const kTechIcon = Icon(
  Icons.laptop,
  size: 100,
);

const kGpsIcon = Icon(
  Icons.location_on,
  size: 100,
);
const kSupportIcon = Icon(
  Icons.headset_mic,
  size: 100,
);

const kBackButtonIcon = Icon(Icons.arrow_back);
const kAddEmployeesText = Text(
  'Add Employees',
  style: TextStyle(color: Colors.black, fontSize: 20.0),
);
